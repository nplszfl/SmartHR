<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smarthr.job.repository.JobRepository">

    <select id="findByConditions" resultType="com.smarthr.job.model.Job">
        SELECT * FROM job WHERE deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="department != null and department != ''">
            AND department = #{department}
        </if>
        <if test="location != null and location != ''">
            AND location = #{location}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="findByStatus" resultType="com.smarthr.job.model.Job">
        SELECT * FROM job WHERE deleted = 0 AND status = #{status} ORDER BY create_time DESC
    </select>

    <select id="findByDepartment" resultType="com.smarthr.job.model.Job">
        SELECT * FROM job WHERE deleted = 0 AND department = #{department} ORDER BY create_time DESC
    </select>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM job WHERE deleted = 0 AND status = #{status}
    </select>

</mapper>